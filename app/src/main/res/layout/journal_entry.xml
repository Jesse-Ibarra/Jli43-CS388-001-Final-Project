<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="?attr/backgroundColor">

    <!-- EditText for Writing Journal Entry -->
    <EditText
        android:id="@+id/editText_journal_entry"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Write your thoughts here..."
        android:inputType="textMultiLine"
        android:gravity="top|start"
        android:minLines="4"
        android:background="@drawable/edittext_background"
        android:textColorHint="@color/gray"
        android:textColor="?attr/colorOnPrimary"
        android:textSize="16sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/button_save_entry" />

    <!-- Button to Save Journal Entry -->
    <Button
        android:id="@+id/button_save_entry"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Save Entry"
        android:textColor="?attr/colorOnPrimary"
        android:backgroundTint="?attr/colorPrimary"
        android:textSize="16sp"
        style="?attr/buttonStyle"
        app:layout_constraintTop_toBottomOf="@id/editText_journal_entry"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/recyclerView_journal_entries" />

    <!-- RecyclerView for Displaying Saved Entries -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView_journal_entries"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/button_save_entry"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>
